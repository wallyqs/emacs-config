;; Anthy (to write in japanese)
(add-to-list 'load-path (concat *wallemacs-root* "modules/anthy/"))

;; Mumamos
(add-to-list 'load-path (concat *wallemacs-root* "modules/mumamos/"))
(require 'ourcomments-util)
(require 'mlinks)
(require 'mumamo-fun)
